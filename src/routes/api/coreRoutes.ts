import { Router } from 'express';
import { } from '../../controllers/CoreController';

const router = Router();


router.get('/:id', () => {});
router.get('/', () => {});
router.post('/', () => {});
router.put('/:id', () => {});
router.delete('/:id', () => {});



export default router;